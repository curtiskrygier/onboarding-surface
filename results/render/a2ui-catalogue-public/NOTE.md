Rendered with `--exposure public` from `../a2ui-catalogue/authored.classified.json`
(the new spec §15 classified-landmine `author` output). Compare with
`../a2ui-catalogue/` (internal, full candour).

- `MAINTAINER-NOTES.md` holds the `exploitable` + `private-ref` landmines — never
  in the three public docs.
- The `landmines` block keeps only `operational` records, with a non-quantified
  footnote (no count, no class names — see spec §15 "Streisand footnote").
